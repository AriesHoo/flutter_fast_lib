name: docxtpl
description: a word document template plugin to easily populate and generate word documents from templates
version: 0.0.1
homepage: https://github.com/DonnC/docxtpl
funding:
 - https://www.paypal.me/donnclab

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.5
  archive: ^3.3.5
  xml: ^5.1.0
  path: ^1.8.0
  collection: ^1.15.0-nullsafety.4

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter: